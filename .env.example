# Base URL of your Strapi backend (including protocol)
VITE_STRAPI_URL=http://localhost:1337

# Optional Strapi API token for authenticated requests
# Generate a read-only token in Strapi (Settings → API Tokens) and paste it below.
# Leave empty if your Strapi collections are publicly accessible.
VITE_STRAPI_TOKEN=
